spring:
  profiles:
    active: test
  application:
    # 根据微服务名称来找动态配置文件
    name: msc-provider-depart
  cloud:
    nacos:
      config:
        server-addr: 192.168.254.128:8848
        file-extension: yml
